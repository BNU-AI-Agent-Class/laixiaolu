# Agent 作业 2 说明

## 选的题目
**选项 A｜给工具箱加一件"搜索"工具(改 c1)——真实开发最高频的动作**
**Capstone 加分题｜真的修好那个 bug**

让 agent 定位并修复 `demo_project` 中 `stats` 命令在“没有任何带标签的笔记”时崩溃的问题。

## 改动内容

只改了一个文件：

- `demo_project/notes_app/analyze.py`

具体改动在 `summary()` 函数里：

```python
counts = tag_counts()
if not counts:
    return f"共有 {len(notes)} 条笔记。\n还没有任何标签。"
top_tag = counts[0][0]
```

- 原来 `counts[0][0]` 在 `counts` 为空列表时会直接抛 `IndexError`。
- 现在先判断 `if not counts`，空标签时给出友好提示，不再崩溃。

## 遇到的最大一个坑

**把 `data/notes.json` 换成一个都不带 `#标签` 的版本,再跑 `python -m notes_app.cli stats`,它会 `IndexError`。**

一开始用 PowerShell 的 `Out-File -Encoding utf8` 写 `notes.json`，结果文件带 BOM，`json.load()` 直接报 `Unexpected UTF-8 BOM`。后来改用 Python 的 `json.dump(..., encoding='utf-8')` 生成无标签测试数据，才成功复现出 `IndexError: list index out of range`。

## 验证结果

- 空标签数据：`python -m notes_app.cli stats` → 正常输出“共有 N 条笔记。还没有任何标签。”
- 原始数据：`python -m notes_app.cli stats` → 正常输出最常用的标签和标签数量。

## 提交材料

详见同仓库 `hw2_capstone/` 目录：

- `analyze.py.diff`：修改前后 diff
- `conversation.md`：与 mini Claude Code 的完整对话过程
- `reflection.md`：对“哪一步最像 / 最不像真 Claude Code”的反思
